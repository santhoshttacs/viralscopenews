{
    "framework": "nextjs",
    "installCommand": "npm install --force",
    "buildCommand": "next build",
    "crons": [
        {
            "path": "/api/fetch-news",
            "schedule": "*/10 * * * *"
        }
    ]
}